public class Bad {
    public void completeWorkout() {
        System.out.println("Workout completed");

        // directly calling every interested party — tightly coupled
      //  AiRecommendationService aiService = new AiRecommendationService();
       // aiService.generateRecommendation();

     //   AnalyticsLogger logger = new AnalyticsLogger();
    //    logger.logCompletion();
    }
}